# 🚀 Eva_Control_Bot - Руководство по развертыванию

Этот документ содержит пошаговые инструкции по развертыванию бота на новом сервере.

---

## 📋 Требования перед развертыванием

### Системные требования

- **ОС**: Linux, macOS или Windows
- **Python**: 3.8 или выше
- **Интернет**: Стабильное соединение для работы с API

### Требуемые учетные данные (API токены)

1. **Telegram Bot Token** - Получить у @BotFather в Telegram
2. **YaWare API Token** - Получить в аккаунте YaWare
3. **PeopleForce API Token** - Получить в аккаунте PeopleForce
4. **Google Service Account** - JSON файл для доступа к Google Sheets
5. **URL Google Sheet** - Ссылка на таблицу для экспорта отчетов

---

## 🔧 Пошаговое развертывание

### Шаг 1: Подготовка сервера

#### На Linux/macOS:

```bash
# Обновить систему
sudo apt update && sudo apt upgrade -y  # (для Linux)

# Установить Python и pip
sudo apt install python3 python3-pip -y  # (для Linux)
# или
brew install python3  # (для macOS)

# Создать рабочую директорию
mkdir -p /opt/eva_control_bot
cd /opt/eva_control_bot
```

#### На Windows:

```cmd
# Скачать Python с https://www.python.org/downloads/
# Убедитесь, что установлен Python 3.8+
# Добавить Python в PATH во время установки

# Создать рабочую директорию
mkdir C:\eva_control_bot
cd C:\eva_control_bot
```

---

### Шаг 2: Загрузить файлы проекта

Скопировать все файлы проекта на сервер:

```bash
# Если используется Git:
git clone <repository-url> .

# Или скопировать файлы напрямую
# Убедитесь, что все файлы находятся в рабочей директории:
# - tracker_alert/ (основной пакет)
# - scripts/ (скрипты)
# - config/ (конфигурация)
# - requirements.txt
# - start_bot.sh (для Linux/macOS)
# - start_bot.bat (для Windows)
```

---

### Шаг 3: Создать файл .env

Создать файл `.env` в корне проекта с необходимыми переменными:

```bash
# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_ADMIN_CHAT_IDS=your_admin_id,second_admin_id

# YaWare API
YAWARE_API_TOKEN=your_yaware_token_here
YAWARE_TIMEZONE=Europe/Warsaw

# PeopleForce API
PEOPLEFORCE_API_TOKEN=your_peopleforce_token_here
PEOPLEFORCE_TIMEZONE=Europe/Warsaw

# Google Sheets
GOOGLE_SHEET_URL=https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID/edit
GOOGLE_CREDENTIALS_PATH=./gcp-sa.json

# Отчеты
REPORT_TIME=08:00
MORNING_MESSAGE_TIME=09:00
TIMEZONE=Europe/Warsaw
```

**Важно!** Никогда не коммитьте `.env` файл в Git!

---

### Шаг 4: Установить Google Sheets учетные данные

1. Скопировать файл `gcp-sa.json` (Google Service Account) в корень проекта
2. Убедиться, что файл содержит правильные координаты доступа

```bash
# Проверить наличие файла
ls -la gcp-sa.json
```

---

### Шаг 5: Установить зависимости Python

```bash
# На Linux/macOS:
pip3 install -r requirements.txt

# На Windows:
pip install -r requirements.txt
```

**Проверить установку:**

```bash
python3 -c "import telegram; print(telegram.__version__)"  # должна вывести версию
```

---

### Шаг 6: Проверить конфигурацию

Убедиться, что все необходимые файлы существуют:

```bash
# На Linux/macOS:
ls -la .env gcp-sa.json config/user_schedules.json config/work_schedules.json requirements.txt

# На Windows:
dir .env gcp-sa.json config\user_schedules.json config\work_schedules.json requirements.txt
```

---

## ▶️ Запуск бота

### На Linux/macOS:

```bash
# Убедиться, что скрипт исполняемый
chmod +x start_bot.sh

# Запустить бота
./start_bot.sh
```

### На Windows:

```cmd
# Двойной клик на start_bot.bat
# Или запустить из CMD:
start_bot.bat
```

**Важно:** Скрипт `start_bot.bat` использует английский текст для совместимости с Windows CMD, которая имеет проблемы с кодировкой кириллицы.

### Проверка успешного запуска:

- Скрипт должен вывести `[OK]` для каждой проверки
- Последнее сообщение: `Starting bot...`
- В консоли должны появиться логи работы бота

---

## ✅ Проверка работоспособности

После запуска бота выполнить следующие тесты:

### 1. Проверка подключения к Telegram

```
/start - Бот должен ответить приветствием с меню
```

### 2. Проверка доступа администратора

```
Убедиться, что администратор может видеть кнопку "⚙️ Админ-панель"
```

### 3. Проверка синхронизации времени

```
Убедиться, что запланированные сообщения отправляются в правильное время:
- 08:00 (по Warsaw): Автоматический отчет
- 09:00 (по Warsaw): Утреннее сообщение с ссылкой на Google Sheets
```

### 4. Проверка API интеграций

```
/отчет - Должны отобразиться данные:
- Опоздавшие пользователи (YaWare)
- Пользователи в отпуске (PeopleForce)
- Пользователи без причины отсутствия (YaWare)
```

### 5. Проверка экспорта

```
Проверить, что еженедельные отчеты экспортируются в Google Sheets
```

---

## 🔄 Регулярное обслуживание

### Мониторинг логов

```bash
# На Linux/macOS - просмотр последних логов:
tail -f eva_bot.log  # если логирование включено

# На Windows - проверять консоль бота
```

### Резервная копия данных

```bash
# Регулярно создавать копию user_schedules.json:
cp config/user_schedules.json config/user_schedules.json.$(date +%Y%m%d).backup
```

### Перезагрузка бота

```bash
# Остановить бота: Ctrl+C

# Перезапустить:
./start_bot.sh  # Linux/macOS
# или
start_bot.bat   # Windows
```

---

## 🚨 Решение проблем

### Проблема: "Python не найден"

```
Решение: Установить Python 3.8+ с https://www.python.org/downloads/
Убедиться, что Python добавлен в PATH
```

### Проблема: "Файл .env не найден"

```
Решение: Создать .env файл в корне проекта с необходимыми переменными
См. Шаг 3 этого документа
```

### Проблема: "Ошибка подключения к API"

```
Решение:
1. Проверить интернет-соединение
2. Проверить корректность токенов в .env
3. Проверить, не истекли ли токены
4. Проверить логи ошибок в консоли
```

### Проблема: "Бот не отвечает на команды"

```
Решение:
1. Убедиться, что бот запущен (ищите "Запуск бота..." в консоли)
2. Убедиться, что TELEGRAM_BOT_TOKEN корректен
3. Проверить, что Telegram имеет доступ в интернет
4. Перезапустить бота
```

### Проблема: "Уведомления не приходят в нужное время"

```
Решение:
1. Проверить правильность TIMEZONE в .env (должна быть Europe/Warsaw)
2. Проверить REPORT_TIME и MORNING_MESSAGE_TIME
3. Убедиться, что сервер имеет правильное системное время
   Команда: date (Linux/macOS) или time (Windows)
4. Перезапустить бота после изменения времени
```

### Проблема: "[OK] is not recognized..." или другие символы вместо текста на Windows

```
Решение: Проблема с кодировкой CMD
1. Убедиться, что используется start_bot.bat (английская версия)
2. Открыть CMD как Администратор
3. Перейти в папку с проектом: cd C:\путь\к\проекту
4. Запустить: start_bot.bat
5. Если проблемы продолжаются:
   - Проверить кодировку консоли: chcp (должна быть 65001)
   - Попробовать: chcp 65001 & start_bot.bat
   - Если не помогает, использовать PowerShell вместо CMD
```

---

## 📞 Контакты поддержки

При возникновении проблем:

1. Проверить логи консоли
2. Проверить корректность данных в .env файле
3. Убедиться, что все API токены активны
4. Обратиться к администратору проекта

---

## 📝 Версионирование

- **Bot Version**: Eva_Control_Bot 1.0
- **Python**: 3.8+
- **python-telegram-bot**: >=20.7
- **Last Updated**: 2024

---

## 🔐 Безопасность

### Важные рекомендации:

- ✅ Никогда не публикуйте `.env` файл
- ✅ Никогда не делитесь токенами и ключами API
- ✅ Используйте сильные пароли для всех сервисов
- ✅ Регулярно обновляйте зависимости Python
- ✅ Регулярно создавайте резервные копии `user_schedules.json`

---

## 📊 Система мониторинга

### Рекомендуемые инструменты:

- **Supervisor** (Linux) - для автоматического перезапуска бота при сбое
- **Cron** - для регулярного резервного копирования данных
- **CloudWatch/Datadog** - для мониторинга здоровья сервера

### Автозапуск на Supervisor (Linux):

```
[program:eva_control_bot]
command=/path/to/eva_control_bot/start_bot.sh
autostart=true
autorestart=true
stderr_logfile=/var/log/eva_control_bot.err.log
stdout_logfile=/var/log/eva_control_bot.out.log
```

---

**Готово к развертыванию! 🎉**
