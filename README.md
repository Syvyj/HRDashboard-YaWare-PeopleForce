# YaWare Productivity Suite

Внутрішня система для аналізу відвідуваності та продуктивності команди, побудована поверх YaWare TimeTracker. Проєкт складається з інтерактивної веб-панелі, експорту у Google Sheets, генерації Excel/PDF звітів і супутніх інструментів автоматизації.

## 🔎 Основні можливості

### Веб-панель управління

- **Dashboard з фільтрами** - аналіз відвідуваності за датами, проєктами, департаментами, командами
- **4-рівнева ієрархія** - Division → Direction → Unit → Team
- **Автоматичне синхронізоване фільтрування** - випадаючі списки адаптуються під обрані значення
- **Multi-select сотрудників** - вибір декількох співробітників для детального аналізу
- **Експорт звітів** - Excel та PDF з повною структурою даних
- **Редагування нотаток** - прямо у веб-інтерфейсі з миттєвим збереженням

### Адміністрування

- **Веб-панель адміністратора** (`/admin`) - управління базою співробітників
- **Синхронізація з PeopleForce** - автоматичне оновлення даних (Division, Direction, Unit, Team, Position, Location)
- **Адаптація даних через Level_Grade.json** - нормалізація ієрархії ("APPS Division" → "Apps")
- **Control Manager Management** - призначення з захистом від перезапису вручну встановлених значень
- **Видалення дат** - очищення помилкових записів за конкретну дату

### Інтеграції

- **YaWare TimeTracker API v2** - автоматичний збір даних продуктивності
- **PeopleForce API** - синхронізація організаційної структури та HR-даних
- **Google Sheets Export** - щоденний та тижневий експорт для аналітики
- **Level_Grade.json** - довідник для коректного мапування ієрархії

## 🧱 Архітектура

| Компонент                     | Каталог                                                       | Опис                                                            |
| ----------------------------- | ------------------------------------------------------------- | --------------------------------------------------------------- |
| Flask dashboard               | `dashboard_app/`, `web_dashboard.py`, `templates/`, `static/` | UI, REST API, авторизація, генерація Excel/PDF                  |
| YaWare → Sheets експортери    | `tracker_alert/`                                              | Клієнт YaWare API, трансформація та запис у Google Sheets       |
| Телеграм/автоматизація        | `tracker_alert/scripts/`                                      | Допоміжні CLI, міграції, алерти                                 |
| Конфігурація робочих графіків | `config/user_schedules.json`, `config/work_schedules.json`    | Картотека користувачів і команд, що використовується у фільтрах |

## 🛠️ Технологічний стек

- **Back-end:** Python 3.10+, Flask, Flask-Login, SQLAlchemy
- **Сховище:** SQLite за замовчуванням (можливий PostgreSQL через `DASHBOARD_DATABASE_URL`)
- **Front-end:** HTML/Jinja, Bootstrap 5, кастомний vanilla JS (`static/js/report.js`)
- **Звіти:** OpenPyXL (Excel), ReportLab (PDF)
- **Інтеграції:** requests + YaWare API v2, Google API Client (Google Sheets), pydantic, python-dotenv
- **Інші утиліти:** click (CLI), pathlib/json для довідників, ruff/black (рекомендовано для lint/format)

Документацію з деплою, API та інтеграцій винесено у каталог `docs/`. Для швидкого огляду розкладів задач, джерел даних і мапінгів використовуйте `docs/AUTOMATION_OVERVIEW.md`, а тематичні гіди (DEPLOYMENT, YAWARE_API_GUIDE, TELEGRAM_BOT_GUIDE тощо) містять деталі по кожному напряму.

## ⚙️ Вимоги

- Python 3.10+
- Google Cloud Service Account з доступом до потрібних таблиць (для експорту)
- Токен YaWare API v2 з доступом до потрібного акаунта
- (Опційно) PeopleForce API, якщо використовується синхронізація графіків

Всі залежності прописані у `requirements.txt`.

## 🚀 Швидкий старт (Dashboard)

1. **Створіть та активуйте віртуальне середовище**

   ```bash
   python3 -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
   ```

2. **Налаштуйте змінні середовища** (можна через `.env` і `python-dotenv`):

   ```bash
   export DASHBOARD_SECRET_KEY="random-secret"
   export DASHBOARD_DATABASE_URL="sqlite:///dashboard.db"  # або ваш PostgreSQL URI

   # YaWare API
   export YAWARE_ACCESS_KEY="..."
   export YAWARE_EMAIL="..."
   export YAWARE_USER_ID="..."
   export YAWARE_BASE_URL="https://data1.yaware.com/export/account/json/v2"

   # Google Sheets (для скриптів експорту)
   export SPREADSHEET_ID="..."
   ```

   Список усіх підтримуваних змінних дивіться у `tracker_alert/config/settings.py`.

3. **Підготуйте Service Account**

   - збережіть `gcp-sa.json` у корені репозиторію
   - надайте доступ до потрібних Google Sheets

4. **Запустіть веб-додаток**

   ```bash
   python web_dashboard.py  # запуск у режимі debug на http://localhost:5000
   ```

5. **Створіть першого користувача**

   ```bash
   flask --app web_dashboard.py create-user admin@example.com "Admin" "StrongPassword" --admin
   ```

   Додатково можна передати `--managers "1,2"`, щоб обмежити доступ даними конкретних керівників.

6. **Підготуйте довідники**

   - `config/user_schedules.json` — база співробітників з 4-рівневою ієрархією:

     - `division_name`, `direction_name`, `unit_name`, `team_name` - канонічні поля
     - `project`, `department`, `unit`, `team` - legacy поля для сумісності
     - `start_time`, `location`, `control_manager`, `peopleforce_id`, `user_id`
     - `_manual_overrides` - флаги полів, встановлених вручну

   - `config/Level_Grade.json` — довідник для адаптації даних:

     - `Division`, `Direction`, `Unit`, `Team`, `Manager`
     - Використовується для нормалізації даних з PeopleForce

   - `config/work_schedules.json` — глобальні налаштування графіків

7. **Налаштуйте інтеграції**

   **PeopleForce API** (опціонально, для синхронізації):

   ```bash
   export PEOPLEFORCE_API_KEY="..."
   export PEOPLEFORCE_BASE_URL="https://api.peopleforce.io/v1"
   ```

   **YaWare API v2** (обов'язково):

   ```bash
   export YAWARE_ACCESS_KEY="..."
   export YAWARE_EMAIL="..."
   export YAWARE_USER_ID="..."
   ```

## 🧭 Робота з фільтрами та ієрархією

### 4-рівнева ієрархія

Система підтримує повну організаційну структуру:

- **Division** (Проект) - Apps, AdNetwork, Agency, Consulting
- **Direction** (Департамент) - Product Team, Sales, Media Buying тощо
- **Unit** (Юніт) - IOS Unit, RTB Team, Creative Unit тощо
- **Team** (Команда) - Development, Hunter Team, Native Team тощо

### Динамічні фільтри

Фільтри на панелі працюють узгоджено:

- Сервер повертає списки допустимих значень з `user_schedules.json`
- При виборі команди автоматично звужуються доступні департаменти/проєкти
- У звітах (Excel/PDF) автоматично заповнюються відповідні поля шапки

### Multi-select співробітників

- Вибір декількох співробітників для детального аналізу
- Пошук по імені у модальному вікні
- Кнопки "Выбрать все" / "Снять выделение"
- Очищення фільтрів зберігає обрані дати

### Синхронізація з PeopleForce

**Автоматична синхронізація**:

```bash
# Масова синхронізація всіх співробітників
curl -X POST http://localhost:5000/api/admin/sync/users
```

**Індивідуальна синхронізація**:

- Відкрити карточку співробітника в `/admin`
- Натиснути "🔄 Синхронізувати з PeopleForce"
- Система оновить Division, Direction, Unit, Team, Position, Location

**Адаптація даних**:

- Після синхронізації натиснути "✨ Адаптировать запись"
- Система використовує `Level_Grade.json` для нормалізації
- Приклад: "APPS Division" → "Apps", заповнення всіх 4 рівнів

## 📄 Експорт у Excel та PDF

- Веб-панель дозволяє відкривати Excel/PDF версії через кнопку «Сформувати звіт».
- Формат таблиці відповідає шаблону `resources/excel/Reports_template/Аркуш2.html`:
  - збережено об’єднання комірок, кольори, стилі заголовків
  - дата відображається у форматі `dd.mm.yy`
  - перший рядок під ім’ям містить чисте значення локації
- В Excel додаються кольорові підсвітки продуктивних колонок та сірий блок `Week total`.

## 📤 Експорт у Google Sheets

Для щоденного/тижневого експорту використовуйте наявні CLI:

```bash
# Щоденний експорт за вказану дату
python3 -m tracker_alert.scripts.export_daily_v2 2025-10-07

# Експорт за вчора (значення за замовчуванням)
python3 -m tracker_alert.scripts.export_daily_v2

# Експорт у власну вкладку
python3 -m tracker_alert.scripts.export_daily_v2 2025-10-07 --sheet custom_tab
```

Додаткові сценарії (тижневий експорт, телеграм бот, алерти) описані у відповідних гайдлах (`AUTO_EXPORT_GUIDE.md`, `TELEGRAM_BOT_GUIDE.md`).

## 📂 Структура репозиторію (скорочено)

```
YaWare_Bot/
├── dashboard_app/             # Flask-додаток, REST API, моделі, scheduler
├── static/                    # JS/CSS для панелі (зокрема filters/report.js)
├── templates/                 # HTML/Jinja шаблони (dashboard, модалки тощо)
├── tracker_alert/             # CLI та інтеграції з YaWare/Google Sheets/Telegram
├── tasks/                     # Оновлення відвідуваності, планувальник
├── config/                    # JSON-довідники користувачів і графіків
├── docs/                      # Керівництва з деплою, API, ботів
├── resources/
│   ├── excel/                 # Референсні Excel-шаблони (`Reports_template*.xlsx`)
│   └── html/                  # HTML-еталони верстки (`Аркуш2_pretty.html`)
├── scripts/                   # Утиліти запуску/експорту
├── web_dashboard.py           # Точка входу Flask (debug)
├── requirements.txt           # Залежності
└── README.md
```

## 🧪 Тестування та форматування

- Покриття тестами мінімальне; для змін у логіці рекомендується додавати інтеграційні тести або smoke-перевірки.
- Перед комітом проганяйте `ruff`/`black` (якщо представлені у вашому робочому середовищі).
- Пам’ятайте про локальні JSON-довідники — змінювати їх краще через окремі PR, щоб легко відстежувати історію.

## 📚 Додаткові матеріали

- `YAWARE_API_GUIDE.md` — огляд можливостей YaWare API
- `DEPLOYMENT.md` — поради щодо розгортання застосунку
- `AUTO_EXPORT_GUIDE.md` — автоматизація та ручні сценарії експорту (включно з тижневим)
- `TELEGRAM_BOT_GUIDE.md` — сповіщення у месенджері
- `AVAILABLE_DATA.md` — опис підготовлених датасетів

## 🤝 Внесок

- Використовуйте відкриті issue/чат для узгодження задач
- Додавайте приклади та скріншоти при зміні UI/форматів звітів
- Під час рев’ю звертайте увагу на консистентність Excel/PDF/Google Sheets — усі три канали мають відображати однакові дані

---

Якщо виникають питання чи потрібні приклади інтеграції з іншими системами, звертайтеся до команди аналітики — вони ведуть основні довідники та автоматизацію.

## 🤖 Telegram Attendance Bot

**Автоматичний моніторинг відвідуваності співробітників**

Бот надсилає щоденні звіти о 10:00 за Варшавським часом про запізнення та відсутності.

### Швидкий старт бота

```bash
# 1. Додайте в .env
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_ADMIN_CHAT_IDS=123456789,987654321

# 2. Встановіть залежності
pip install -r requirements.txt

# 3. Запустіть бота
python scripts/run_attendance_bot.py
```

### Команди бота

- `/start` - Привітання
- `/help` - Довідка
- `/status` - Статус системи
- `/report_today` - Звіт за сьогодні

**Детальна документація:** [TELEGRAM_BOT_GUIDE.md](TELEGRAM_BOT_GUIDE.md)

## 📝 Ліцензія

Internal / Proprietary

## 🤝 Підтримка

При виникненні питань щодо API - звертайтесь до YaWare Support.
