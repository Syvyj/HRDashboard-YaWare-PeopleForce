# ⚙️ Узагальнення автоматизацій

Цей документ описує, як взаємодіють Telegram‑бот, веб‑дашборд, PeopleForce та Google Sheets. Він допомагає швидко зорієнтуватися, де знаходяться налаштування, розклади та що є «джерелом правди» для кожного типу даних.

## 🤖 Рівні автоматизації

| Компонент | Що робить | Головні файли |
| --- | --- | --- |
| **Telegram Attendance Bot** | Ручні команди (`/report_today`, `/status`), щоденні звіти о 10:02 та 09:32 (Пн–Пт). | `tracker_alert/bot/telegram_bot.py`, `tracker_alert/bot/scheduler.py`, `scripts/run_attendance_bot.py`, `docs/TELEGRAM_BOT_GUIDE.md` |
| **Scheduler бота** | 10:02 — повний звіт «ОТЧЕТ ПО ОПОЗДАНИЯМ» (тільки якщо є запізнення/відсутні); 09:32 — коротке повідомлення з кнопкою на дашборд. | `tracker_alert/bot/scheduler.py`, `docs/AUTO_EXPORT_GUIDE.md` |
| **Scheduler веб-додатка** | О 10:00 — синк **запізнень** за сьогодні (lateness_records); о 09:30 — синк вчора (attendance); PeopleForce о 09:17. **Працює лише якщо `ENABLE_SCHEDULER=1`**. | `dashboard_app/tasks.py`, `lateness_service.py`, `tasks/update_attendance.py` |
| **Документація** | README + цей файл → загальна картина; спеціалізовані гіди пояснюють деталі (Telegram, PeopleForce, експорт). | `README.md`, `docs/*.md` |

## 🕒 Розклад задач та часові пояси

| Час Warsaw | Дні | Завдання | Відповідальний компонент |
| --- | --- | --- | --- |
| 09:14 | Щодня | Бекап БД перед синком (якщо scheduler увімкнено). | `_backup_database` |
| 09:17 | Щодня | Оновлення статусів PeopleForce (відпустка/лікарняний) у `user_schedules.json`. | `_sync_peopleforce_metadata` |
| 10:00 | Пн‑Пт | Синхронізація **запізнень за сьогодні** з YaWare → `lateness_records` (лише запізнення; та сама логіка, що кнопка «Синхронізувати день» на Опоздания). **Потрібно `ENABLE_SCHEDULER=1`.** | `_run_today_lateness_sync` → `collect_lateness_for_date` (`dashboard_app/tasks.py`, `lateness_service.py`) |
| 10:02 | Пн‑Пт | Telegram: повний звіт «ОТЧЕТ ПО ОПОЗДАНИЯМ» з даних `lateness_records` (запізнення сьогодні); якщо запізнень/відсутностей немає — нічого не відправляється. | `AttendanceScheduler.send_full_report` |
| 09:30 | Щодня | Синхронізація **вчорашнього** дня YaWare → `attendance_records`. **Потрібно `ENABLE_SCHEDULER=1`.** | `_run_daily_attendance` (`dashboard_app/tasks.py`) |
| 09:32 | Пн‑Пт | Telegram: коротке повідомлення з кнопкою на дашборд (dbrd.ctrlbot.website). | `AttendanceScheduler.send_short_report` |

> **Примітка:** усі Cron-задачі використовують часову зону Europe/Warsaw, тому переходи на літній/зимовий час обробляються автоматично.

**Чому автосинхронізація може не працювати:** завдання о 09:14 / 09:17 / 09:30 / 10:00 запускаються **планувальником веб-додатка** (Flask/gunicorn). Він увімкнений лише якщо в оточенні задано `ENABLE_SCHEDULER=1`. О 09:30 — повний синк **вчорашнього** дня (`update_for_date` → `attendance_records`); о 10:00 — лише **синк запізнень** за сьогодні (`collect_lateness_for_date` → `lateness_records`). Звіт бота о 10:02 бере дані з `lateness_records`.

## 🗂️ Джерела даних та власники

| Дані | Джерело правди | Оновлює | Коментарі |
| --- | --- | --- | --- |
| API ключі, ID таблиць, чатів | `.env` / `tracker_alert/config/settings.py` | DevOps (секрети), власники інтеграцій | Єдине місце для YAWARE/PeopleForce/Sheets/Telegram токенів. |
| Графіки користувачів, manual overrides | `config/user_schedules.json` | Адміни (ручні правки), PeopleForce-синк | Автосинк змінює лише поля без manual-флагів. |
| Контроль‑менеджери | `user_schedules.json` (`control_manager`), довідник у PeopleForce | Scheduler та адміністратори | Використовується для фільтрів у дашборді та часткових Telegram-звітів. |
| Відвідуваність & продуктивність | YaWare API v2 | Scheduler (дашборд/бот), ручні CLI (`tasks/update_attendance.py`) | Після імпорту зберігається у SQLite (`instance/dashboard.db`). |
| Відпустки/лікарняні статуси | PeopleForce API | `_sync_peopleforce_metadata`, `scripts/sync_peopleforce_telegram.py` | Автоматично проставляється тільки leave status та сума дня. |
| Google Sheets | `tracker_alert/services/sheets.py` + `gcp-sa.json` | Scheduler, ручні запуски експорту | Таблиці поділяються між менеджерами (див. нижче). |

## 👥 Мапінг контроль‑менеджерів та Google Sheets

| ID менеджера | Що означає | Доступ у Telegram | Таблиця Google Sheets |
| --- | --- | --- | --- |
| `1` | Контрольна група #1 (Apps/Control 1) | Налаштовується через `TELEGRAM_MANAGER_MAPPING` (`chat_id:1`). | `SPREADSHEET_ID_CONTROL_1` |
| `2` | Контрольна група #2 | `TELEGRAM_MANAGER_MAPPING` (`chat_id:2`). | `SPREADSHEET_ID_CONTROL_2` |
| — | Повний доступ (всі менеджери) | Відсутність мапінгу або кілька ID | `SPREADSHEET_ID` (основна таблиця) |

**Де використовується:**

- `AttendanceScheduler` фільтрує звіт і посилання на таблиці, спираючись на `AttendanceBot.get_allowed_managers`.
- У веб-дашборді control manager визначає, які дані бачить користувач, і чи належить запис до певного сегмента.
- PeopleForce-синк використовує спільну утиліту `tracker_alert.services.control_manager.auto_assign_control_manager`.

## 🛠️ Шпаргалка запусків (CLI + Cron)

### CLI-команди

- **Запуск Telegram-бота + scheduler:** `python scripts/run_attendance_bot.py`
- **Створити/оновити тижневий аркуш:** `python3 -m tracker_alert.scripts.export_weekly [YYYY-MM-DD]`
- **Додати вчорашній день у поточний аркуш:** `python3 -m tracker_alert.scripts.update_yesterday`
- **Ручний синк PeopleForce → user_schedules.json:** `python3 scripts/sync_peopleforce_telegram.py`
- **Імпорт відвідуваності в дашборд:** `python3 tasks/update_attendance.py --date 2025-01-15` (або з діапазоном `--start-date/--end-date`)

Команди можна запускати з кореня репозиторію; за потреби додайте `PYTHONPATH` чи `.env`.

### Приклади cron

```cron
# О 09:00 щодня додавати вчорашній день (update_yesterday)
0 9 * * * cd /opt/YaWare_Bot && /usr/bin/python3 -m tracker_alert.scripts.update_yesterday >> /var/log/yaware/update.log 2>&1

# О 08:00 щопонеділка створювати новий тиждень (export_weekly)
0 8 * * 1 cd /opt/YaWare_Bot && /usr/bin/python3 -m tracker_alert.scripts.export_weekly >> /var/log/yaware/export.log 2>&1
```

### Приклад systemd timer (щоденний update_yesterday)

`/etc/systemd/system/yaware-update.service`

```ini
[Unit]
Description=YaWare Daily Update
After=network.target

[Service]
Type=oneshot
User=deploy
WorkingDirectory=/opt/YaWare_Bot
ExecStart=/usr/bin/python3 -m tracker_alert.scripts.update_yesterday
StandardOutput=journal
StandardError=journal
```

`/etc/systemd/system/yaware-update.timer`

```ini
[Unit]
Description=YaWare Daily Update Timer
Requires=yaware-update.service

[Timer]
OnCalendar=*-*-* 09:00:00
Persistent=true

[Install]
WantedBy=timers.target
```

Активувати:

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now yaware-update.timer
```

## 📚 Як читати документацію

1. **Прагнете швидко згадати розклад/інтеграції?** — читати цей файл.
2. **Потрібні деталі про Telegram-команди, запуск бота чи troubleshooting?** — `docs/TELEGRAM_BOT_GUIDE.md`.
3. **Налаштовуєте PeopleForce → Telegram/менеджерів?** — `docs/PEOPLEFORCE_API_GUIDE.md`.
4. **Хочете зрозуміти експорт у Sheets або перезапустити вручну?** — `docs/AUTO_EXPORT_GUIDE.md`.
5. **Шукаєте загальне бачення проєкту та фронтенд/бекенд?** — `README.md` + `docs/PROJECT_STRUCTURE.md`.

Цей огляд допоможе уникнути дублювання інформації між файлами. Якщо в інших документах описано розклад або мапінг, посилайтеся на таблиці вище.
