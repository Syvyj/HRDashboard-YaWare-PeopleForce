# 🤖 Автоматичний експорт в Google Sheets

## 📋 Загальна інформація

Історично експорт у Google Sheets виконувався автоматично. Зараз планувальник вимкнено: дані акумулюються безпосередньо на сайті, а цей гайд описує, як запустити експорт вручну (наприклад, для резервної копії). Розклад інших автоматизацій наведений у `docs/AUTOMATION_OVERVIEW.md`.

## ⏰ Розклад роботи

Автоматичний експорт вимкнений. Якщо знадобиться повернути регулярні завантаження, використовуйте приклади cron/systemd нижче.

## 📅 Логіка експорту

### Понеділок-П'ятниця (Вт-Пт):

- Експортується **вчорашній день**
- Приклад: У вівторок 15.10 експортується понеділок 14.10

### Понеділок:

- Експортується **п'ятниця минулого тижня**
- Приклад: У понеділок 14.10 експортується п'ятниця 11.10
- Автоматично створюється новий тиждень якщо його ще немає

## 🔄 Створення нового тижня

Коли експортується п'ятниця і потрібен новий тиждень:

1. Автоматично створюється новий аркуш (наприклад "Week 42 (07-11 Oct 2025)")
2. Створюється каркас на всі 5 днів тижня (Пн-Пт)
3. Заповнюється тільки п'ятниця минулого тижня
4. Інші дні залишаються порожніми і будуть заповнені протягом тижня

## 📊 Структура експорту

Кожен день експортується в формат:

```
Ім'я користувача | Email | Локація | Графік | День1 | День2 | ... | День5
```

## 🔍 Що експортується

- ✅ Час початку роботи
- ✅ Статус (вчасно/запізнення)
- ✅ Продуктивний час
- ✅ Непродуктивний час
- ✅ Відпустки з PeopleForce (автоматично помічаються кольором)
- ✅ Локації співробітників

## ⚙️ Технічні деталі

### Файли:

- `tracker_alert/bot/scheduler.py` - Планувальник задач
- `tracker_alert/scripts/export_weekly.py` - Скрипт експорту

### Конфігурація:

```python
EXPORT_TIME = time(8, 0)  # 08:00
REPORT_TIMEZONE = "Europe/Warsaw"
day_of_week='mon-fri'  # Тільки робочі дні
```

### Логи:

Всі операції логуються в консоль бота:

- `📅 Monday detected: exporting last Friday`
- `📅 Exporting yesterday: 2025-10-14`
- `✅ Export completed successfully`

## 🚨 Моніторинг

Бот автоматично відстежує:

- Timeout експорту (максимум 5 хвилин)
- Помилки експорту
- Статус виконання

Всі помилки логуються з повним stack trace.

## 📝 Приклади

### Сценарій 1: Вівторок 15.10.2025

```
08:00 - Експорт понеділка 14.10.2025
Дані додаються в існуючий Week 42
```

### Сценарій 2: Понеділок 14.10.2025

```
08:00 - Експорт п'ятниці 11.10.2025
Створюється Week 42 (якщо не існує)
Заповнюється тільки п'ятниця
Інші дні (Пн-Чт) порожні
```

### Сценарій 3: Субота 12.10.2025

```
08:00 - Експорт НЕ виконується
Лог: "⏭️  Skipping export on weekend"
```

## 🔧 Ручний запуск експорту

Якщо потрібно експортувати вручну:

```bash
python3 -m tracker_alert.scripts.export_weekly 2025-10-14
```

## ✅ Статус

Scheduler запущений і працює:

```
Scheduler started:
  - Telegram reports: 10:00 Europe/Warsaw
  - Sheets export: 08:00 Europe/Warsaw (Mon-Fri)
```

## 📁 Структура проєкту

```
tracker_alert/
├── client/
│   └── yaware_v2_api.py       # YaWare API v2 клієнт
├── config/
│   └── settings.py            # Налаштування (.env)
├── domain/
│   ├── week_utils.py          # Утиліти для роботи з тижнями
│   └── weekly_mapping.py      # Форматування блоків користувачів
├── services/
│   └── sheets.py              # Google Sheets API
└── scripts/
    ├── export_weekly.py       # Створення тижневого аркушу
    └── update_yesterday.py    # Щоденне додавання вчорашньої дати
```

## 🔧 Налаштування

1. **Залежності**

   ```bash
   pip install -r requirements.txt
   ```

2. **.env**

   ```env
   YAWARE_ACCESS_KEY=your_access_key_here
   YAWARE_BASE_URL=https://data1.yaware.com/export/account/json/v2

   SPREADSHEET_ID=your_spreadsheet_id_here
   SHEET_TAB=by_user
   SA_PATH=gcp-sa.json
   ```

3. **Service Account**

   Розмістіть `gcp-sa.json` у корені проєкту та дайте сервіс-акаунту доступ до відповідних таблиць.

## 📖 Використання CLI

### Створення тижневого аркушу

```bash
# Поточний тиждень
python3 -m tracker_alert.scripts.export_weekly

# Будь-яка дата тижня
python3 -m tracker_alert.scripts.export_weekly 2025-10-06
```

**Що відбувається:** визначається тижневий діапазон, пропускається незавершений день, збираються дані з YaWare, формуються блоки користувачів, створюється/оновлюється аркуш (наприклад, `Week 41 (06-10 Oct 2025)`), застосовується форматування.

### Щоденне оновлення (додавання вчорашнього дня)

```bash
python3 -m tracker_alert.scripts.update_yesterday
```

**Що відбувається:** береться вчорашня дата (або п’ятниця, якщо понеділок), шукається актуальний аркуш, підтягуються дані з YaWare, оновлюються рядки користувачів та перераховуються Totals.

## 🤖 Автоматизація

Rозширені приклади cron/systemd наведені нижче; коротку шпаргалку див. у `docs/AUTOMATION_OVERVIEW.md#🛠️-шпаргалка-запусків-cli--cron`.

### Cron (Linux/macOS)

```cron
0 9 * * * cd /path/to/YaWare_Bot && python3 -m tracker_alert.scripts.update_yesterday >> /path/to/logs/update.log 2>&1
0 8 * * 1 cd /path/to/YaWare_Bot && python3 -m tracker_alert.scripts.export_weekly >> /path/to/logs/export.log 2>&1
```

### systemd timer

`/etc/systemd/system/yaware-update.service`

```ini
[Unit]
Description=YaWare Daily Update
After=network.target

[Service]
Type=oneshot
User=your_user
WorkingDirectory=/path/to/YaWare_Bot
ExecStart=/usr/bin/python3 -m tracker_alert.scripts.update_yesterday
StandardOutput=journal
StandardError=journal
```

`/etc/systemd/system/yaware-update.timer`

```ini
[Unit]
Description=YaWare Daily Update Timer
Requires=yaware-update.service

[Timer]
OnCalendar=*-*-* 09:00:00
Persistent=true

[Install]
WantedBy=timers.target
```

```
sudo systemctl daemon-reload
sudo systemctl enable yaware-update.timer
sudo systemctl start yaware-update.timer
```

## 📊 Формат даних у Google Sheets

```
| Name | Plan Start | Data | Fact Start | Non Productive | Not Categorized | Productive | Total | Screenshots | Notes |
|------|------------|------|------------|----------------|-----------------|------------|-------|-------------|-------|
| Іван Петренко, ivan@company.com | Plan Start: | | | | | | | | |
| | | 06.10.2025 | 09:15 | 0.51 | 0.25 | 7.73 | 8.49 | | |
| | | 07.10.2025 | 09:22 | 0.48 | 0.23 | 7.65 | 8.36 | | |
| | | 08.10.2025 | 09:18 | 0.52 | 0.26 | 7.71 | 8.49 | | |
| | | 09.10.2025 | 09:25 | 0.49 | 0.24 | 7.68 | 8.41 | | |
| | | 10.10.2025 | 09:20 | 0.50 | 0.25 | 7.70 | 8.45 | | |
| Total | | | | 2.50 | 1.23 | 38.47 | 42.20 | | |
```

Кожен користувач має власний блок: заголовок, п’ять днів, рядок `Total` та порожній розділювач. Відпустки з PeopleForce фарбуються кольорами автоматично.
