# Аналіз файлів проекту для публікації на GitHub

## ✅ Файли, які правильно виключені через .gitignore

### Чутливі дані (в .gitignore):
- ✅ `.env`, `.env.*` - змінні середовища
- ✅ `gcp-sa.json` - Google Service Account ключ
- ✅ `config/user_schedules.json` - база співробітників з реальними даними
- ✅ `config/work_schedules.json` - конфігурація графіків
- ✅ `instance/*.db` - бази даних
- ✅ `instance/week_notes.json`, `instance/monthly_notes.json` - нотатки
- ✅ `logs/*.log` - логи
- ✅ `backups/` - бекапи
- ✅ `All_Backup/` - великі бекапи

### Тимчасові файли (в .gitignore):
- ✅ `__pycache__/`, `*.pyc` - Python кеш
- ✅ `.venv/` - віртуальне середовище
- ✅ `*.swp`, `*.swo`, `*~` - IDE тимчасові файли
- ✅ `.DS_Store` - macOS системні файли
- ✅ `*.backup`, `*.backup_*` - бекапи

---

## ⚠️ Файли, які потребують уваги

### 1. `config/yaware.service` ⚠️

**Статус:** Файл systemd service unit

**Проблема:** Може містити шляхи до сервера або специфічні налаштування

**Рекомендація:**
- Перевірити на наявність шляхів/IP адрес
- Якщо містить специфічні дані - видалити або створити `.example` версію
- Або перенести в `archive/` якщо не використовується

**Дія:** Перевірити та видалити/перенести якщо містить чутливі дані

---

### 2. `archive/temp_data/Level_Grade.xlsx` ⚠️

**Статус:** Excel файл з даними

**Проблема:** Може містити реальні дані співробітників

**Рекомендація:**
- Перевірити на наявність реальних даних
- Якщо містить чутливі дані - видалити
- Або залишити якщо це тільки структура без даних

**Дія:** Перевірити та видалити якщо містить чутливі дані

---

### 3. Міграційні скрипти в `scripts/` ⚠️

**Статус:** Багато одноразових міграційних скриптів

**Файли:**
- `add_pf_status_column.py`
- `add_record_type_column.py`
- `add_half_day_amount_column.py`
- `add_indexes.py`
- `add_internal_ids.py`
- `add_is_control_manager_column.py`
- `migrate_control_manager_to_text.py`
- `convert_presets_to_internal_ids.py`
- `fill_internal_user_ids.py`
- `fix_missing_internal_user_ids.py`
- `fix_scheduled_start_from_config.py`
- `populate_employee_ids.py`
- `update_internal_user_ids.py`
- `remove_duplicate_daily_records.py`
- `create_employee_table.py`

**Проблема:** Це одноразові скрипти міграцій, які вже виконані

**Рекомендація:**
- Залишити якщо можуть знадобитися для нових інсталяцій
- Або перенести в `archive/scripts/` якщо точно не потрібні
- Або видалити якщо міграції вже застосовані і не повторюються

**Дія:** Залишити якщо можуть знадобитися, інакше перенести в `archive/`

---

### 4. `archive/scripts/` ⚠️

**Статус:** Старі скрипти в archive

**Файли:**
- `apply_inheritance.py`
- `fix_team_structure.py`

**Рекомендація:**
- Залишити в `archive/` - це історичні файли
- Або видалити якщо точно не потрібні

**Дія:** Залишити в `archive/` (це нормально для історичних файлів)

---

### 5. `archive/temp_data/` ⚠️

**Статус:** Тимчасові дані

**Проблема:** Може містити реальні дані

**Рекомендація:**
- Перевірити вміст
- Видалити якщо містить чутливі дані
- Або додати в `.gitignore` якщо потрібно зберегти структуру

**Дія:** Перевірити та видалити чутливі дані

---

### 6. `.agent/workflows/deploy.md` ⚠️

**Статус:** Workflow файл для агента

**Проблема:** Може містити специфічні шляхи/команди

**Рекомендація:**
- Перевірити на наявність чутливих даних
- Оновити якщо містить специфічні дані

**Дія:** Перевірити (вже оновлено IP адреси)

---

## 📋 Рекомендації по .gitignore

### Додати до .gitignore (якщо потрібно):

```gitignore
# Archive temp data (якщо містить чутливі дані)
archive/temp_data/*.xlsx
archive/temp_data/*.json

# Service files (якщо містить специфічні дані)
*.service

# Excel файли з даними (якщо є)
*.xlsx
*.xls

# IDE
.idea/
.vscode/
*.code-workspace
```

---

## ✅ Файли, які можна залишити

### Корисні скрипти (залишити):
- `scripts/backup_database.sh` - корисний для бекапів
- `scripts/deploy_code.sh`, `scripts/deploy_full.sh` - корисні для деплою
- `scripts/sync_from_server.sh` - корисний для синхронізації
- `scripts/run_attendance_bot.py` - основний скрипт запуску
- `scripts/sync_peopleforce_telegram.py` - корисний для синхронізації
- `scripts/cleanup_*.sh` - корисні для очищення

### Міграційні скрипти (залишити якщо можуть знадобитися):
- Залишити якщо проект може встановлюватися з нуля
- Перенести в `archive/` якщо міграції вже застосовані і не повторюються

---

## 🎯 План дій

1. ⏳ Перевірити `config/yaware.service` на чутливі дані
2. ⏳ Перевірити `archive/temp_data/Level_Grade.xlsx` на чутливі дані
3. ⏳ Вирішити що робити з міграційними скриптами
4. ⏳ Оновити `.gitignore` якщо потрібно

---

**Дата створення:** 2025-02-04
