# Місячні звіти - Логіка розрахунків та приклади

Цей документ описує логіку розрахунків у місячних звітах та містить приклади для розуміння.

## 📊 Джерела даних

- **БД** `attendance_records`: записи по дням (дата, статус, хвилини, leave_reason, half_day_amount тощо)
- **Файл** `instance/monthly_adjustments.json`: ручні правки по `internal_user_id` та місяцю (plan_days, vacation_days, fact_days, minimum_hours, start_date тощо)
- **Файл** `instance/monthly_notes.json`: текстові нотатки за місяць по користувачу

## 🔢 Формули розрахунків

### 1. Plan Days (Плановані дні)

Кількість робочих днів у місяці (Пн–Пт), при необхідності від `start_date` до кінця місяця.

**Функція:** `_count_work_days_in_month(year, month, include_weekends, start_date)`

- Якщо в `monthly_adjustments` заданий `start_date` для користувача — рахуються дні починаючи з цієї дати
- Для співробітників з 7-денним тижнем (`include_weekends=True`) рахуються всі дні

### 2. Відпустки/Відсутності (leave_data)

По записах за місяць зі статусом `leave`, `absent`, `late`:

- **vacation_days** — відпустка (по leave_reason: vacation, отпуск, відпустка тощо), інакше за замовчуванням відпустка
- **day_off_days** — за свій рахунок / вихідний (свой счет, day off, выходной тощо) + всі дні зі статусом `absent` або `late` (рахуються як day off)
- **sick_days** — лікарняний (sick, больничный, лікарняний)

**Важливо:** Враховується `half_day_amount` (0.5 або 1.0). Вихідні (Сб/Нд) не рахуються для співробітників без 7-денного тижня.

### 3. Fact Days (Фактичні дні)

```
fact_days = plan_days - vacation_days - day_off_days - sick_days
```

### 4. Minimum Hours (Мінімальні години)

За замовчуванням: `fact_days * min_hours_per_day`

- `min_hours_per_day`: 6.5 для Agency/Apps, 7.0 для Adnetwork/Consulting (та за замовчуванням)
- Можна перевизначити в `monthly_adjustments` (`hours_per_day`)

**Формат у відповіді API:** `"ЧЧ:ММ"` (хвилини переведені в години)

### 5. Tracked Hours (Відстежені години)

Сума по денним записам: `(not_categorized_minutes + productive_minutes)` за місяць.

**Важливо:** Записи `record_type == 'week_total'` в цю суму не входять.

**Формат у відповіді:** `"ЧЧ:ММ"`

### 6. Corrected Hours (Скориговані години)

Сума `corrected_total_minutes` по записах, де це поле задано і `record_type != 'week_total'`.

**Формат у відповіді:** `"ЧЧ:ММ"`

### 7. Delay Count (Кількість запізнень)

Кількість записів за місяць, де `minutes_late > 10`.

## ✏️ Ручні корекції

Всі значення, розраховані вище, можуть бути перезаписані з `monthly_adjustments.json` для даної пари (internal_user_id, місяць):

- plan_days
- vacation_days
- day_off_days
- sick_days
- fact_days
- minimum_hours
- delay_count
- tracked_hours
- та інші

## 💻 Frontend (monthly_report.js)

### Функція `recalculateAutoFields()` (модалка редагування)

- Якщо задані місяць та **Start Date** — перерахунок **Plan Days**: робочі дні (Пн–Пт) від Start Date до кінця місяця
- **Fact Days** = Plan Days − Vacation − Day Off − Sick
- **Minimum Hours** = Fact Days × вибране «Годин на день» (6.5 або 7), формат `ЧЧ:ММ`

Збереження відправляється в `/api/monthly-adjustments` і потрапляє в `monthly_adjustments.json`. При наступному завантаженні звіту ці значення підставляються в картку співробітника.

---

## 📝 Приклади розрахунків

### Приклад 1: Example User, січень

#### Правильний ручний розрахунок (еталон)

| Поле                  | Значення |
| --------------------- | -------- |
| **Plan Days**         | 21       |
| **Vacation**          | 4        |
| **Day Off**           | 0        |
| **Sick**              | 0        |
| **Fact Days**         | 17       |
| **Minimum per month** | 110:30   |
| **Tracked Hours**     | 125:49   |
| **Corrected Hours**   | 118:54   |

#### Перевірка логіки системи

- **Plan Days = 21** — січень з виключенням 1 січня (FIXED_HOLIDAYS_MD), без `start_date`
- **Fact Days** = Plan Days − Vacation − Day Off − Sick = 21 − 4 − 0 − 0 = **17** ✓
- **Minimum per month** = Fact Days × годин на день. У Example User проєкт **Apps** → 6.5 год/день:  
  **17 × 6.5 = 110.5 год = 110:30** ✓ (збігається зі скріншотом)
- **Tracked Hours** та **Corrected Hours** — з БД (суми за січень)

Ручних корекцій у `monthly_adjustments.json` для Example User (internal_id 126) за січень немає — усі значення мають збігатися з автоматичним розрахунком.

#### Перевірка по БД (реальні суми)

Запит по `attendance_records` за січень (у прикладі використано **2026-01**):

- Умови: `user_name = 'Example User'` (або email/user_id), `record_date` у межах місяця, тільки **робочі дні** (Пн–Пт, `strftime('%w', record_date)` від 1 до 5), тільки записи **daily** (без `week_total`)
- **Tracked** = сума `(not_categorized_minutes + productive_minutes)` по таких записах
- **Corrected** = сума `corrected_total_minutes` по таких записах (де `corrected_total_minutes IS NOT NULL`)

Результат по БД за січень 2026:

| Метрика   | Хвилини | Формат   |
| --------- | ------- | -------- |
| Tracked   | 7549    | 125:49 ✓ |
| Corrected | 7134    | 118:54 ✓ |

**Підсумок:** Розрахунок для Example User за січень повністю збігається з ручним еталоном: Plan Days 21, Vacation 4, Fact Days 17, Minimum 110:30, **Tracked 125:49**, **Corrected 118:54** (підтверджено по БД за січень 2026).

---

### Приклад 2: Example User з Day Off, січень

#### Правильний ручний розрахунок (еталон)

| Поле                  | Значення |
| --------------------- | -------- |
| **Plan Days**         | 21       |
| **Vacation**          | 0        |
| **Day Off**           | 6        |
| **Sick**              | 0        |
| **Fact Days**         | 15       |
| **Minimum per month** | 65:00    |
| **Tracked Hours**     | 41:59    |
| **Corrected Hours**   | 24:16    |

#### Чому Plan Days = 21, а не 22

У січні календарних днів 31, з них понеділок–п'ятниця без виключень було б 22 робочих дні. Але **1 січня — неробочий день для всіх** (Новий рік). Якщо система рахувала лише «пон–пт» і не виключала 1 січня, виходило 22. Після правок у коді:

- У `dashboard_app/constants.py` додано **FIXED_HOLIDAYS_MD**: `(1, 1)` (Новий рік), `(1, 7)` (Різдво)
- У `_count_work_days_in_month()` ці дати **не рахуються** як робочі

Тому для січня без `start_date` тепер: **Plan Days = 21** (22 − 1 січня).

#### Формули

- **Fact Days** = Plan Days − Vacation − Day Off − Sick  
  Приклад: 21 − 0 − 6 − 0 = **15**

- **Minimum per month** (у нашій системі) = Fact Days × годин на день (наприклад 6.5 або 7 за проєктом)  
  У ручному еталоні мінімум 65:00 — можливо, окреме правило (наприклад, фіксований мінімум за місяць); у коді мінімум рахується як Fact × hours_per_day

- **Tracked Hours** — сума з YaWare (productive + not_categorized) за місяць
- **Corrected Hours** — сума скоригованих годин із записів за місяць

#### Day Off = 6

Ці 6 днів у січні для Example User — це дні з нашими статусами **absent** або **late**, або **leave** з причиною типу «day off» / «за свой счёт» тощо. Вони беруться з БД (attendance_records) за січень і підсумовуються в поле Day Off у звіті за місяць.

---

## 🔍 Додаткові примітки

### Вихідні дні

Вихідні (субота/неділя) не рахуються як робочі дні для співробітників без 7-денного тижня (`include_weekends=False`).

### Half Day Amount

Поле `half_day_amount` дозволяє враховувати часткові дні:
- `0.5` — півдня
- `1.0` — повний день

### Ручні корекції

Всі значення можуть бути перевизначені в `monthly_adjustments.json` для конкретного користувача та місяця. Це дозволяє враховувати особливі випадки, які автоматичний розрахунок не покриває.

---

**Останнє оновлення:** 2025-02-04
