# Синхронізація даних - Довідник

Документація по синхронізації даних з YaWare та PeopleForce, включаючи обробку відпусток заднім числом та рекомендації.

## 📋 Зміст

- [Синхронізація відпусток заднім числом](#синхронізація-відпусток-заднім-числом)
- [Синхронізація перед відкриттям звіту](#синхронізація-перед-відкриттям-звіту)

---

## Синхронізація відпусток заднім числом

### Проблема

Щоденна синхронізація на сервері виконується **один раз на день за вчорашній день** (09:30 Warsaw). Відпустки та лікарняні приходять з **PeopleForce** в момент виклику `update_for_date(target_date)`: для `target_date` запитуються `get_leave_requests(start_date=target_date, end_date=target_date)`.

Якщо HR виставляє відпустку/лікарняний **заднім числом** (вже після того, як день пройшов і синк виконався), в нашій базі запис за той день не оновлюється — залишається старий статус (absent без leave_reason або present), і розрахунки в місячному звіті (vacation_days, sick_days, fact_days) виходять некоректними.

### Варіанти рішення

#### 1. Періодичний пересинк за останні N днів (рекомендується)

Додати задачу в планувальник, яка раз на тиждень (наприклад, в неділю вранці) пересинхронізує відвідуваність за останні 7–14 днів. Тоді будь-які відпустки/лікарняні, оформлені заднім числом протягом тижня, підтягнуться при наступному запуску.

**Плюси:**
- Автоматично підхоплює заднім числом без дій користувача

**Мінуси:**
- Додаткове навантаження на API PeopleForce/YaWare (кілька дат раз на тиждень)

**Ручні правки:**
- Зберігаються — при `update_for_date` ми не чіпаємо записи з manual overrides (вони відновлюються зі знімка)

#### 2. Ручний пересинк за діапазон дат (вже є)

Скрипт `tasks/update_attendance.py` вже підтримує діапазон:

```bash
# Пересинк за період (наприклад, коли HR вніс відпустку заднім числом)
python tasks/update_attendance.py --start-date 2025-01-01 --end-date 2025-01-15
```

Цю ж логіку можна викликати з адмінки (кнопка «Пересинхронізувати за період» з вибором дат), якщо потрібно дати можливість не заходити на сервер.

#### 3. Щоденний пересинк за останні 2–3 дні

Замість одного запуску тільки за вчора — кожен день запускати синк за вчора, позавчора і (опціонально) день перед цим. Тоді «заднє число» протягом 1–2 днів після дати все одно підтягнеться.

**Плюси:**
- Швидше відпрацьовує недавнє заднє число

**Мінуси:**
- Кожен день 2–3 дати замість однієї — більше запитів до API

### Що зроблено

1. **Щотижневий пересинк** — в планувальник додано задачу **неділя 10:00 Warsaw**: пересинхронізація відвідуваності за останні 7 днів (від вчора назад). Відпустки/лікарняні, оформлені заднім числом за тиждень, підтягнуться при наступному недільному запуску.

2. **Ручний пересинк** — вже можливий через CLI (див. вище); при терміновій необхідності можна виконати пересинк за будь-який період без очікування неділі.

---

## Синхронізація перед відкриттям звіту

### Ідея

Перед тим як менеджер або адмін побачить сторінку «Звіт за місяць», в фоні запускати синхронізацію даних (attendance за останні дні місяця та/або статуси з PeopleForce), щоб цифри в звіті були актуальними.

### Плюси

- При відкритті звіту дані вже «свіжі» — включно з відпустками/лікарняними, оформленими недавно або заднім числом
- Не потрібно вручну натискати «Оновити» або чекати наступного ранкового cron

### Ризики та нюанси

1. **Час відповіді**  
   Синк по API (YaWare, PeopleForce) за кілька дат може займати 10–30+ секунд. Якщо робити його **до** віддачі HTML/даних, сторінка буде відкриватися з затримкою, що погано для UX.

2. **Варіанти реалізації**
   - **A) Синк до завантаження сторінки**  
     При переході на `/monthly-report` спочатку на бекенді запускаємо синк (наприклад, за поточний місяць або за останні N днів), потім віддаємо сторінку. Мінус: довгий перший відгук.
   - **B) Синк у фоні після відкриття**  
     Сторінка одразу віддається і завантажує звіт з кешу/БД; паралельно через API запускається фонова задача «пересинк за поточний місяць». Після завершення можна показати сповіщення «Дані оновлено» і запропонувати перезавантажити звіт. Плюс: швидкий перший показ; мінус: користувач може не натиснути «Оновити».
   - **C) «М'який» синк тільки по PeopleForce**  
     Не перезбирати все з YaWare, а тільки підтягнути відпустки/лікарняні з PeopleForce за вибраний місяць і оновити наші записи. Швидше, ніж повний пересинк.

3. **Дублювання з cron**  
   Вранці вже запускається щоденний синк за вчора і раз на тиждень — бекфілл за 7 днів. Синк при відкритті звіту дає додаткову свіжість, але може створювати зайве навантаження на API при частих відкриттях.

4. **Хто і коли**  
   Має сенс запускати такий синк тільки при відкритті звіту за **поточний** або **попередній** місяць і тільки для ролей з правом перегляду звіту (менеджер/адмін). Для старих місяців можна не запускати синк.

### Рекомендація (без коду)

Оптимально виглядає варіант **B**: відкриття сторінки без затримки, дані за місяць підвантажуються як зараз; окремо (по кнопці «Оновити дані за місяць» або автоматично при першому заході на сторінку) в фоні запускати пересинк за вибраний місяць. Після завершення — тост/повідомлення «Дані оновлено» і кнопка «Показати актуальні дані» (перезапит звіту з сервера). Так ми не блокуємо інтерфейс і даємо користувачу явно підтягнути актуальні відпустки/лікарняні.

Альтернатива — залишити як є і покладатися на щотижневий бекфілл + ручний пересинк за період при необхідності; тоді доопрацювання «синк перед звітом» не обов'язкова.

---

## 🔧 Практичні приклади

### Ручний пересинк за період

```bash
# Пересинк за конкретний період
python tasks/update_attendance.py --start-date 2025-01-01 --end-date 2025-01-15

# Пересинк за останні 7 днів
python tasks/update_attendance.py --start-date $(date -d '7 days ago' +%Y-%m-%d) --end-date $(date -d 'yesterday' +%Y-%m-%d)
```

### Перевірка статусів відпусток

```python
from tracker_alert.client.peopleforce_api import PeopleForceClient
from datetime import date, timedelta

client = PeopleForceClient()

# Перевірити відпустки за останній тиждень
today = date.today()
start = today - timedelta(days=7)
end = today

leaves = client.get_leave_requests(start, end)
print(f"Знайдено {len(leaves)} відпусток за період {start} - {end}")
```

---

## ⚠️ Важливі примітки

1. **Manual overrides зберігаються** — при пересинку не перезаписуються записи з ручними правками
2. **Навантаження на API** — пересинк за великий період може створювати значне навантаження
3. **Кешування** — PeopleForce API кешує дані на 5 хвилин, тому часті пересинки можуть не давати оновлених даних

---

**Останнє оновлення:** 2025-02-04
