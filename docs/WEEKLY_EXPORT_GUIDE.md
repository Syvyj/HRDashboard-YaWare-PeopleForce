# 📊 YaWare Weekly Export - Інструкція

## Що робить система

Система експортує тижневі дані з YaWare TimeTracker до Google Sheets у форматі **блоків користувачів**:

- Один аркуш = один робочий тиждень (Пн-Пт)
- Один блок на користувача = заголовок + 5 днів + Total + роздільник
- Автоматичне виключення поточного дня (експортуються тільки завершені дні)

## 📁 Структура проєкту

```
tracker_alert/
├── client/
│   └── yaware_v2_api.py       # YaWare API v2 клієнт
├── config/
│   └── settings.py            # Налаштування (.env)
├── domain/
│   ├── week_utils.py          # Утиліти для роботи з тижнями
│   └── weekly_mapping.py      # Форматування блоків користувачів
├── services/
│   └── sheets.py              # Google Sheets API
└── scripts/
    ├── export_weekly.py       # Створення тижневого аркушу
    └── update_yesterday.py    # Щоденне додавання вчорашньої дати
```

## 🔧 Налаштування

### 1. Встановити залежності

```bash
pip install -r requirements.txt
```

### 2. Налаштувати .env

```env
# YaWare API v2
YAWARE_ACCESS_KEY=your_access_key_here
YAWARE_BASE_URL=https://data1.yaware.com/export/account/json/v2

# Google Sheets
SPREADSHEET_ID=your_spreadsheet_id_here
SHEET_TAB=by_user
SA_PATH=gcp-sa.json
```

### 3. Додати Service Account файл

Створити `gcp-sa.json` з ключами Google Service Account

## 📖 Використання

### Створення тижневого аркушу

```bash
# Експорт за поточний тиждень
python3 -m tracker_alert.scripts.export_weekly

# Експорт за конкретний тиждень (будь-яка дата з того тижня)
python3 -m tracker_alert.scripts.export_weekly 2025-10-06
```

**Що робить:**

- Визначає понеділок і п'ятницю тижня
- Виключає сьогоднішню дату (якщо день ще триває)
- Робить 1-5 API запитів (один на кожен завершений день)
- Групує дані по користувачам
- Створює/оновлює аркуш з назвою "Week 41 (06-10 Oct 2025)"
- Застосовує форматування (жирний текст, фони, автоширина колонок)

**Вивід:**

```
📅 Експорт тижневої статистики
   Тиждень: 2025-10-06 - 2025-10-07
   Аркуш: 'Week 41 (06-10 Oct 2025)'
🔍 Отримуємо дані з YaWare API за 2 днів...
✅ Зібрано дані за тиждень для 171 користувачів
🔄 Форматуємо дані для експорту...
✅ Згенеровано 850 рядків
📤 Створюємо аркуш...
🎨 Застосовуємо форматування...
📊 Статистика експорту:
   Користувачів: 171
   Днів: 2
   Рядків: 850
🎉 Експорт завершено успішно!
```

### Щоденне оновлення (додавання вчорашньої дати)

```bash
# Додати дані за вчорашній день до поточного тижневого аркушу
python3 -m tracker_alert.scripts.update_yesterday
```

**Що робить:**

- Визначає вчорашню дату (пропускає вихідні)
- Знаходить відповідний тижневий аркуш
- Отримує дані за вчора з YaWare API
- Оновлює відповідні рядки для кожного користувача
- Перераховує рядки Total
- Пропускає користувачів, у яких дані вже є

**Вивід:**

```
📅 Оновлення даних за 07.10.2025 (Tuesday)
   Аркуш: 'Week 41 (06-10 Oct 2025)'
✅ Аркуш знайдено
🔍 Отримуємо дані з YaWare API...
✅ Отримано дані для 167 користувачів
📖 Зчитуємо існуючий аркуш...
📍 День тижня: Tuesday (день #2)
💾 Оновлюємо 165 користувачів (пропущено: 0)...
✅ Оновлення завершено!
🔢 Перераховуємо Total...
🎉 Готово!
```

## 🤖 Автоматизація

### Cron (Linux/macOS)

```bash
# Редагувати crontab
crontab -e

# Додати рядок (щодня о 09:00)
0 9 * * * cd /path/to/YaWare_Bot && python3 -m tracker_alert.scripts.update_yesterday >> /path/to/logs/update.log 2>&1

# Додати рядок (щопонеділка о 08:00 - створення нового тижневого аркушу)
0 8 * * 1 cd /path/to/YaWare_Bot && python3 -m tracker_alert.scripts.export_weekly >> /path/to/logs/export.log 2>&1
```

### systemd Timer (Linux)

**Файл: /etc/systemd/system/yaware-update.service**

```ini
[Unit]
Description=YaWare Daily Update
After=network.target

[Service]
Type=oneshot
User=your_user
WorkingDirectory=/path/to/YaWare_Bot
ExecStart=/usr/bin/python3 -m tracker_alert.scripts.update_yesterday
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

**Файл: /etc/systemd/system/yaware-update.timer**

```ini
[Unit]
Description=YaWare Daily Update Timer
Requires=yaware-update.service

[Timer]
OnCalendar=*-*-* 09:00:00
Persistent=true

[Install]
WantedBy=timers.target
```

**Активувати:**

```bash
sudo systemctl daemon-reload
sudo systemctl enable yaware-update.timer
sudo systemctl start yaware-update.timer
```

## 📊 Формат даних в Google Sheets

### Структура аркушу

```
| Name | Plan Start | Data | Fact Start | Non Productive | Not Categorized | Productive | Total | Screenshots | Notes |
|------|------------|------|------------|----------------|-----------------|------------|-------|-------------|-------|
| Іван Петренко, ivan@company.com | Plan Start: | | | | | | | | |
| | | 06.10.2025 | 09:15 | 0.51 | 0.25 | 7.73 | 8.49 | | |
| | | 07.10.2025 | 09:22 | 0.48 | 0.23 | 7.65 | 8.36 | | |
| | | 08.10.2025 | 09:18 | 0.52 | 0.26 | 7.71 | 8.49 | | |
| | | 09.10.2025 | 09:25 | 0.49 | 0.24 | 7.68 | 8.41 | | |
| | | 10.10.2025 | 09:20 | 0.50 | 0.25 | 7.70 | 8.45 | | |
| Total | | | | 2.50 | 1.23 | 38.47 | 42.20 | | |
| | | | | | | | | | |
| Марія Коваленко, maria@company.com | Plan Start: | | | | | | | | |
...
```

### Поля

- **Name**: Ім'я та email користувача
- **Plan Start**: Плановий час початку (порожнє, чекаємо API від підтримки)
- **Data**: Дата в форматі DD.MM.YYYY
- **Fact Start**: Фактичний час початку роботи (з API)
- **Non Productive**: Непродуктивний час (годин)
- **Not Categorized**: Некатегоризований час (годин)
- **Productive**: Продуктивний час (годин)
- **Total**: Загальний час (годин)
- **Screenshots**: Кількість скріншотів (порожнє, ручне заповнення)
- **Notes**: Нотатки (порожнє, ручне заповнення)

## 🔍 Діагностика

### Перевірити що повертає API

```bash
python3 -c "
from tracker_alert.client.yaware_v2_api import YaWareV2Client
client = YaWareV2Client()
data = client.get_summary_by_day('2025-10-07')
print(f'Користувачів: {len(data)}')
print('Перший запис:', data[0] if data else 'немає даних')
"
```

### Перевірити налаштування

```bash
python3 -c "
from tracker_alert.config.settings import Settings
s = Settings()
print(f'Spreadsheet ID: {s.spreadsheet_id}')
print(f'Access Key: {s.yaware_access_key[:10]}...')
"
```

### Подивитися логи аркушів

```bash
python3 -c "
from tracker_alert.services.sheets import _service
from tracker_alert.config.settings import settings
service = _service()
meta = service.spreadsheets().get(spreadsheetId=settings.spreadsheet_id).execute()
sheets = [s['properties']['title'] for s in meta.get('sheets', [])]
print('Аркуші:', sheets)
"
```

## ⚠️ Відомі обмеження

1. **Plan Start** - поле порожнє, чекаємо на API endpoint від YaWare Support
2. **Screenshots** - поле порожнє, немає API доступу (ручне заповнення)
3. **Поточний день** - виключається автоматично (експортуються тільки завершені дні)
4. **Вихідні** - автоматично пропускаються (Субота, Неділя)
5. **Total** - автоматично перераховується при додаванні нових днів

## 📝 Changelog

### v1.0 (8 жовтня 2025)

- ✅ Створення тижневих аркушів з блоками користувачів
- ✅ Автоматичне виключення поточного дня
- ✅ Щоденне оновлення (додавання вчорашньої дати)
- ✅ Автоматичний перерахунок Total
- ✅ Форматування (жирний текст, фони, автоширина)
- ✅ CLI скрипти для експорту та оновлення

## 🆘 Підтримка

При виникненні проблем:

1. Перевірити `.env` файл
2. Перевірити права Service Account в Google Sheets
3. Подивитися логи (`python3 -m tracker_alert.scripts.export_weekly`)
4. Перевірити що API ключ YaWare працює

## 📜 Ліцензія

Internal use only
